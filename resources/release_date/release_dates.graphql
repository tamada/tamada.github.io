# This is just an example query file.
# used query is generated by query_builder.sh script.
query {
  repo01: repository(owner: "tamada", name: "sibling") {
    ...repoFields
  }
  repo02: repository(owner: "tamada", name: "totebag") {
    ...repoFields
  }
  repo03: repository(owner: "tamada", name: "pick-a-boo") {
    ...repoFields
  }
  repo04: repository(owner: "tamada", name: "gixor") {
    ...repoFields
  }
  repo05: repository(owner: "tamada", name: "oinkie") {
    ...repoFields
  }
  repo06: repository(owner: "tamada", name: "btmeister") {
    ...repoFields
  }
  repo07: repository(owner: "tamada", name: "pochi") {
    ...repoFields
  }
  repo08: repository(owner: "tamada", name: "wildcat") {
    ...repoFields
  }
  repo09: repository(owner: "tamada", name: "9rules") {
    ...repoFields
  }
  repo10: repository(owner: "tamada", name: "goheatman") {
    ...repoFields
  }
}

fragment repoFields on Repository {
  name
  url
  latestRelease {
    publishedAt
    url
    tagName
  }
}
